# Fuctions
select count(price) as Count from transactions;
select min(price) as min from transactions;
select max(price) as max from transactions;
select avg(price) as avg from transactions;
select sum(price) as sum from transactions;

select * from employees
select concat(first_name, ' ', last_name) as full_name from employees;